# owp-sdk changelog

## v0.1.0

- Initial publishable module extraction from `platform/go`
- `searchdef.Problem` — stable BYOD engine contract
- `sdk.RegisterProblem` — domain loader registry
- `ProblemDescriptor` solve UI fields (`Title`, `PolicyDomain`, `ObjectiveLabel`) for generic `owp solve` without cmd hooks
